/**
 * Cuenta la cantidad de formas de subir una escalera de N escalones. Por
 * defecto podemos subir los escalones de uno en uno o de dos en dos, pero en
 * la versión avanzada podemos configurar la longitud de los pasos
 *
 * @param {number} numberSteps número de escalones
 * @param {number[]} stepSizes longitud de los pasos
 * @returns {number} la cantidad de formas de subir la escalera
 */
function numWays(numberSteps, stepSizes = [1, 2]) {
  // TODO: implement
}

module.exports = numWays
